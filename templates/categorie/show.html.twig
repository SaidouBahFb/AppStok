{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block pageTitle %}
        Details catégorie
{% endblock %}
{% block body %}

    <table class="table table-striped table-bordered">
            <div class="my-2 offset-10">
            <a class="btn btn-secondary" href="{{ path('app_categorie_index') }}">retour à la liste</a>
                
            </div>
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ categorie.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ categorie.nom }}</td>
                </tr>
            </tbody>
        </table>
        <div class = "d-flex align-items-center">        
            <a class="mt-1  btn btn-warning offset-5" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a> 
            <a class="mx-2 btn ">{{ include('categorie/_delete_form.html.twig') }}</a>
        </div>
        
    </table>
    
{% endblock %}
